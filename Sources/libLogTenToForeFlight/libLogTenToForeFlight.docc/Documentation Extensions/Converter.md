# ``libLogTenToForeFlight/Converter``

@Metadata {
    @DocumentationExtension(mergeBehavior: append)
}

## Overview

The `Converter` class transforms LogTen Pro logbook data into ForeFlight-compatible format. It handles the complex mapping of aircraft types, crew roles, and flight data between the two systems.

## Topics

### Creating a Converter

- ``init(logbook:)``

### Performing Conversion

- ``convert()``
